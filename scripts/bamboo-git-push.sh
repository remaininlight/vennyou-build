#!/bin/bash

echo "Git add, commit, push"

cd ../../vennyou-app

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
echo $DIR 

#git add -A
#git commit -m "[bamboo] Build from website"
#git push
